import { BackendDecision, ExternalPolicyBackend } from '../types';
interface OPABackendConfig {
    endpoint: string;
    policyPath?: string;
    fetchImpl?: typeof fetch;
}
export declare class OPABackend implements ExternalPolicyBackend {
    readonly name = "opa";
    private readonly endpoint;
    private readonly policyPath;
    private readonly fetchImpl;
    constructor(config: OPABackendConfig);
    evaluateAction(action: string, context: Record<string, unknown>): Promise<BackendDecision>;
    evaluatePolicy(agentDid: string, context: Record<string, unknown>): Promise<BackendDecision>;
}
export {};
//# sourceMappingURL=opa.d.ts.map