export interface X25519KeyPair {
    privateKey: Uint8Array;
    publicKey: Uint8Array;
}
export interface PreKeyBundle {
    identityKey: Uint8Array;
    /** Ed25519 signing public key — required for signature verification. */
    identityKeyEd: Uint8Array;
    signedPreKey: Uint8Array;
    signedPreKeySignature: Uint8Array;
    signedPreKeyId: number;
    oneTimePreKey?: Uint8Array;
    oneTimePreKeyId?: number;
}
export interface X3DHResult {
    sharedSecret: Uint8Array;
    ephemeralPublicKey: Uint8Array;
    usedOneTimeKeyId?: number;
    associatedData: Uint8Array;
}
export declare function generateX25519KeyPair(): X25519KeyPair;
export declare function ed25519ToX25519(ed25519Private: Uint8Array, ed25519Public: Uint8Array): X25519KeyPair;
export declare class X3DHKeyManager {
    readonly identityKey: X25519KeyPair;
    private readonly ed25519Private;
    private readonly ed25519Public;
    private signedPreKeyPair;
    private oneTimePreKeys;
    private nextSpkId;
    private nextOtkId;
    constructor(ed25519Private: Uint8Array, ed25519Public: Uint8Array);
    /**
     * Ed25519 public key used to sign pre-keys. Distinct from
     * `identityKey.publicKey` (X25519). Peers MUST be given this key to
     * verify the signature on the signed pre-key (`verifyBundle`).
     */
    get identityKeyEd(): Uint8Array;
    generateSignedPreKey(): {
        keyId: number;
        publicKey: Uint8Array;
        signature: Uint8Array;
    };
    generateOneTimePreKeys(count: number): Array<{
        keyId: number;
        publicKey: Uint8Array;
    }>;
    getPublicBundle(otkId?: number): PreKeyBundle;
    initiate(peerBundle: PreKeyBundle): X3DHResult;
    respond(peerIdentityKey: Uint8Array, ephemeralPublicKey: Uint8Array, usedOneTimeKeyId?: number): X3DHResult;
    get signedPreKey(): {
        keyPair: X25519KeyPair;
        signature: Uint8Array;
        keyId: number;
    } | null;
}
//# sourceMappingURL=x3dh.d.ts.map