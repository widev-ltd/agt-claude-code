/**
 * SecureChannel — high-level E2E encrypted agent-to-agent messaging.
 *
 * Spec: docs/specs/AGENTMESH-WIRE-1.0.md Section 9
 */
import { EncryptedMessage } from "./ratchet";
import { X3DHKeyManager, PreKeyBundle } from "./x3dh";
export interface ChannelEstablishment {
    initiatorIdentityKey: Uint8Array;
    ephemeralPublicKey: Uint8Array;
    usedOneTimeKeyId?: number;
}
export declare class SecureChannel {
    private ratchet;
    private associatedData;
    private closed;
    private msgCount;
    readonly localIdentityKey: Uint8Array;
    readonly remoteIdentityKey: Uint8Array;
    private constructor();
    static createSender(keyManager: X3DHKeyManager, peerBundle: PreKeyBundle, associatedData?: Uint8Array): [SecureChannel, ChannelEstablishment];
    static createReceiver(keyManager: X3DHKeyManager, establishment: ChannelEstablishment, associatedData?: Uint8Array): SecureChannel;
    send(plaintext: Uint8Array): EncryptedMessage;
    receive(message: EncryptedMessage): Uint8Array;
    close(): void;
    get isClosed(): boolean;
    get messageCount(): number;
}
//# sourceMappingURL=channel.d.ts.map