"use strict";
// Copyright (c) Microsoft Corporation. Licensed under the MIT License.
Object.defineProperty(exports, "__esModule", { value: true });
exports.DockerSandboxProvider = exports.ExecutionStatus = exports.SessionStatus = void 0;
exports.defaultSandboxConfig = defaultSandboxConfig;
const child_process_1 = require("child_process");
const crypto_1 = require("crypto");
// ---------------------------------------------------------------------------
// Enums
// ---------------------------------------------------------------------------
var SessionStatus;
(function (SessionStatus) {
    SessionStatus["Provisioning"] = "provisioning";
    SessionStatus["Ready"] = "ready";
    SessionStatus["Executing"] = "executing";
    SessionStatus["Destroying"] = "destroying";
    SessionStatus["Destroyed"] = "destroyed";
    SessionStatus["Failed"] = "failed";
})(SessionStatus || (exports.SessionStatus = SessionStatus = {}));
var ExecutionStatus;
(function (ExecutionStatus) {
    ExecutionStatus["Pending"] = "pending";
    ExecutionStatus["Running"] = "running";
    ExecutionStatus["Completed"] = "completed";
    ExecutionStatus["Cancelled"] = "cancelled";
    ExecutionStatus["Failed"] = "failed";
})(ExecutionStatus || (exports.ExecutionStatus = ExecutionStatus = {}));
// ---------------------------------------------------------------------------
// Default config factory
// ---------------------------------------------------------------------------
function defaultSandboxConfig() {
    return {
        timeoutSeconds: 60,
        memoryMb: 512,
        cpuLimit: 1.0,
        networkEnabled: false,
        readOnlyFs: true,
        envVars: {},
    };
}
// ---------------------------------------------------------------------------
// Environment variables blocked from sandbox containers
// ---------------------------------------------------------------------------
const BLOCKED_ENV_VARS = new Set([
    'LD_PRELOAD',
    'LD_LIBRARY_PATH',
    'LD_AUDIT',
    'LD_DEBUG',
    'LD_PROFILE',
    'LD_SHOW_AUXV',
    'LD_DYNAMIC_WEAK',
    'PYTHONSTARTUP',
    'PYTHONPATH',
]);
function sanitizeEnvVars(envVars) {
    const clean = {};
    for (const [k, v] of Object.entries(envVars)) {
        if (!BLOCKED_ENV_VARS.has(k.toUpperCase())) {
            clean[k] = v;
        }
    }
    return clean;
}
// Docker resource-name pattern: [a-zA-Z0-9][a-zA-Z0-9_.-]{0,127}
const DOCKER_NAME_RE = /^[a-zA-Z0-9][a-zA-Z0-9_.-]{0,127}$/;
function validateResourceName(value, label) {
    if (!DOCKER_NAME_RE.test(value)) {
        throw new Error(`Invalid ${label} '${value}': must match [a-zA-Z0-9][a-zA-Z0-9_.-]{0,127}`);
    }
}
// ---------------------------------------------------------------------------
// DockerSandboxProvider
// ---------------------------------------------------------------------------
class DockerSandboxProvider {
    image;
    containers = new Map(); // sessionId -> containerId
    constructor(image = 'python:3.11-slim') {
        this.image = image;
    }
    async isAvailable() {
        try {
            (0, child_process_1.execSync)('docker info', { stdio: 'pipe', timeout: 10_000 });
            return true;
        }
        catch {
            return false;
        }
    }
    async createSession(agentId, config) {
        validateResourceName(agentId, 'agentId');
        const cfg = config ?? defaultSandboxConfig();
        const sessionId = (0, crypto_1.randomUUID)();
        const containerName = `agt-${agentId}-${sessionId.slice(0, 8)}`;
        const args = [
            'run', '-d',
            '--name', containerName,
            '--cap-drop=ALL',
            '--security-opt=no-new-privileges',
            `--memory=${cfg.memoryMb}m`,
            `--cpus=${cfg.cpuLimit}`,
            '--pids-limit=256',
        ];
        if (cfg.readOnlyFs) {
            args.push('--read-only');
        }
        if (!cfg.networkEnabled) {
            args.push('--network=none');
        }
        const safeEnv = sanitizeEnvVars(cfg.envVars);
        for (const [k, v] of Object.entries(safeEnv)) {
            args.push('-e', `${k}=${v}`);
        }
        args.push(this.image, 'tail', '-f', '/dev/null');
        try {
            const containerId = (0, child_process_1.execFileSync)('docker', args, {
                stdio: 'pipe',
                timeout: 30_000,
            })
                .toString()
                .trim();
            this.containers.set(sessionId, containerId);
            return {
                agentId,
                sessionId,
                status: SessionStatus.Ready,
            };
        }
        catch (err) {
            const message = err instanceof Error ? err.message : String(err);
            throw new Error(`Failed to create sandbox session: ${message}`);
        }
    }
    async executeCode(agentId, sessionId, code) {
        validateResourceName(agentId, 'agentId');
        const containerId = this.containers.get(sessionId);
        if (!containerId) {
            throw new Error(`No active session '${sessionId}' for agent '${agentId}'`);
        }
        const executionId = (0, crypto_1.randomUUID)();
        const startTime = Date.now();
        return new Promise((resolve) => {
            const encoded = Buffer.from(code).toString('base64');
            const execArgs = [
                'exec', containerId, 'python3', '-c',
                `import base64; exec(base64.b64decode('${encoded}').decode())`,
            ];
            (0, child_process_1.execFile)('docker', execArgs, { timeout: 60_000 }, (error, stdout, stderr) => {
                const durationSeconds = (Date.now() - startTime) / 1000.0;
                // Node's ExecException.code can be: a numeric exit code (child exited
                // non-zero), `null` (child killed by a signal — `error.signal` is set
                // instead), or a string like 'ENOENT' (the spawn itself failed). The
                // previous `error.code as number ?? 1` cast handled the `null` case
                // by accident — `null ?? 1` is `1` — but on the spawn-failure path it
                // let the string ('ENOENT') flow through as a `number`-typed exit
                // code, and downstream consumers treating `exitCode` as numeric saw
                // a string. Narrow explicitly: only accept a numeric code; otherwise
                // synthesise 1 for any error (signal kill, spawn failure, etc.).
                const exitCode = error
                    ? typeof error.code === 'number' ? error.code : 1
                    : 0;
                const killed = error !== null && 'killed' in error && error.killed;
                const result = {
                    success: exitCode === 0,
                    exitCode,
                    stdout: stdout ?? '',
                    stderr: stderr ?? '',
                    durationSeconds,
                    killed,
                    killReason: killed ? 'timeout' : '',
                };
                resolve({
                    executionId,
                    agentId,
                    sessionId,
                    status: exitCode === 0 ? ExecutionStatus.Completed : ExecutionStatus.Failed,
                    result,
                });
            });
        });
    }
    async destroySession(agentId, sessionId) {
        validateResourceName(agentId, 'agentId');
        const containerId = this.containers.get(sessionId);
        if (!containerId) {
            return; // already destroyed or never existed
        }
        try {
            (0, child_process_1.execFileSync)('docker', ['rm', '-f', containerId], {
                stdio: 'pipe',
                timeout: 15_000,
            });
        }
        finally {
            this.containers.delete(sessionId);
        }
    }
}
exports.DockerSandboxProvider = DockerSandboxProvider;
//# sourceMappingURL=sandbox.js.map