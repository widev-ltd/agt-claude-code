export interface MessageHeader {
    dhPublicKey: Uint8Array;
    previousChainLength: number;
    messageNumber: number;
}
export interface EncryptedMessage {
    header: MessageHeader;
    ciphertext: Uint8Array;
}
export interface RatchetState {
    dhSelfPrivate: Uint8Array;
    dhSelfPublic: Uint8Array;
    dhRemotePublic: Uint8Array | null;
    rootKey: Uint8Array;
    chainKeySend: Uint8Array | null;
    chainKeyRecv: Uint8Array | null;
    sendMessageNumber: number;
    recvMessageNumber: number;
    previousSendChainLength: number;
    skippedKeys: Map<string, Uint8Array>;
}
export declare class DoubleRatchet {
    private state;
    private maxSkip;
    private constructor();
    static initSender(sharedSecret: Uint8Array, remoteDHPublic: Uint8Array, maxSkip?: number): DoubleRatchet;
    static initReceiver(sharedSecret: Uint8Array, dhKeyPair: {
        privateKey: Uint8Array;
        publicKey: Uint8Array;
    }, maxSkip?: number): DoubleRatchet;
    encrypt(plaintext: Uint8Array, associatedData?: Uint8Array): EncryptedMessage;
    decrypt(message: EncryptedMessage, associatedData?: Uint8Array): Uint8Array;
    private dhRatchetStep;
    private skipMessages;
    getState(): RatchetState;
    static fromState(state: RatchetState, maxSkip?: number): DoubleRatchet;
}
//# sourceMappingURL=ratchet.d.ts.map