"use strict";
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
Object.defineProperty(exports, "__esModule", { value: true });
exports.RingEnforcer = exports.RingBreachError = void 0;
const types_1 = require("./types");
class RingBreachError extends Error {
    violation;
    constructor(violation) {
        super(violation.message);
        this.name = 'RingBreachError';
        this.violation = violation;
    }
}
exports.RingBreachError = RingBreachError;
class RingEnforcer {
    agentRing;
    defaultRing;
    actionRings;
    constructor(config = {}) {
        this.agentRing = config.agentRing ?? types_1.ExecutionRing.Ring3;
        this.defaultRing = config.defaultRing ?? types_1.ExecutionRing.Ring3;
        this.actionRings = config.actionRings ?? {};
    }
    getAgentRing() {
        return this.agentRing;
    }
    getRequiredRing(action) {
        for (const [pattern, ring] of Object.entries(this.actionRings)) {
            if (this.matches(pattern, action)) {
                return ring;
            }
        }
        return this.defaultRing;
    }
    canExecute(action) {
        return this.agentRing <= this.getRequiredRing(action);
    }
    enforce(action) {
        if (this.canExecute(action)) {
            return;
        }
        throw new RingBreachError({
            action,
            agentRing: this.agentRing,
            requiredRing: this.getRequiredRing(action),
            message: `Execution ring breach for action "${action}": agent ring ${this.agentRing} exceeds required ring ${this.getRequiredRing(action)}`,
        });
    }
    matches(pattern, action) {
        if (pattern === '*') {
            return true;
        }
        if (pattern.endsWith('*')) {
            return action.startsWith(pattern.slice(0, -1));
        }
        return pattern === action;
    }
}
exports.RingEnforcer = RingEnforcer;
//# sourceMappingURL=rings.js.map