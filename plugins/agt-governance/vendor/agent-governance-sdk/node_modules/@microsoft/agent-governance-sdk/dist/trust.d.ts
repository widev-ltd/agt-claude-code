import { AgentIdentity } from './identity';
import { TrustConfig, TrustScore, TrustVerificationResult } from './types';
/**
 * Manages trust scores for peer agents using a simple Bayesian-inspired model
 * with configurable decay.
 */
export declare class TrustManager {
    private readonly config;
    private readonly agents;
    private readonly persistPath?;
    constructor(config?: TrustConfig);
    /** Verify a peer agent's identity and return a trust result. */
    verifyPeer(peerId: string, peerIdentity: AgentIdentity): Promise<TrustVerificationResult>;
    /** Get the current trust score for an agent. */
    getTrustScore(agentId: string): TrustScore;
    /** Record a successful interaction with an agent. */
    recordSuccess(agentId: string, reward?: number): void;
    /** Record a failed interaction with an agent. */
    recordFailure(agentId: string, penalty?: number): void;
    private getOrCreateState;
    private applyDecay;
    private computeTier;
    private computeReliability;
    private getPeerVerificationFailure;
    private saveToDisk;
    private loadFromDisk;
}
//# sourceMappingURL=trust.d.ts.map