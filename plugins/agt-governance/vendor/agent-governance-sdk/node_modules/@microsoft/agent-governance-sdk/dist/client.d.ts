import { AgentIdentity } from './identity';
import { TrustManager } from './trust';
import { PolicyEngine } from './policy';
import { AuditLogger } from './audit';
import { AgentMeshConfig, GovernanceResult } from './types';
import { KillSwitch } from './kill-switch';
import { LifecycleManager } from './lifecycle';
import { RingEnforcer } from './rings';
/**
 * Unified client that ties identity, trust, policy, and audit together
 * into a single governance-aware entry point.
 */
export declare class AgentMeshClient {
    readonly identity: AgentIdentity;
    readonly trust: TrustManager;
    readonly policy: PolicyEngine;
    readonly audit: AuditLogger;
    readonly lifecycle: LifecycleManager;
    readonly ringEnforcer: RingEnforcer;
    readonly killSwitch?: KillSwitch;
    private readonly quarantineOnBreach;
    private readonly killOnBreach;
    constructor(config: AgentMeshConfig);
    /** Convenience factory. */
    static create(agentId: string, options?: Partial<AgentMeshConfig>): AgentMeshClient;
    /**
     * Execute an action through the full governance pipeline:
     * 1. Evaluate policy
     * 2. Check trust score
     * 3. Log to audit trail
     */
    executeWithGovernance(action: string, params?: Record<string, unknown>): Promise<GovernanceResult>;
}
//# sourceMappingURL=client.d.ts.map